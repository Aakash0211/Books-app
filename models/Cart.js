class Cart{
  constructor(id,ownerId,title,authors,language,quantity){
    this.id=id,
    this.ownerId=ownerId,
    this.title=title,
    this.authors=authors,
    this.language=language,
    this.quantity=quantity
  }
}
export default Cart