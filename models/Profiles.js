class Profiles{
    constructor(userId,imageUri,name,number,bio){
     this.userId=userId,
     this.imageUri=imageUri,
     this.name=name,
     this.number=number,
     this.bio=bio
    }
}
export default Profiles